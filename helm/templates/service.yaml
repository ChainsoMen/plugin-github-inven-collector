apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-inven-collector" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 50051
  selector:
    app: {{ include "github-inven-collector" . }}